# Backend API origin used by the Vite development server/browser.
# Keep /api/v1 in the frontend request module; this value is only the origin.
VITE_API_BASE_URL=http://localhost:8000

# Vite development server
VITE_DEV_HOST=127.0.0.1
VITE_DEV_PORT=5173
